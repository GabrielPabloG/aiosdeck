[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "hello-python"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["requests>=2.31"]

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tests"]

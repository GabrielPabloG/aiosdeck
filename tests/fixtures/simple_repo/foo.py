# foo.py intentionally lacks a module docstring.

# TODO: replace this placeholder with real auth logic.
# FIXME: revisit token expiry handling.


def process(items):
    # XXX: inline note about ordering
    result = []
    for item in items:
        result.append(item)
    return result
